1. Create the Secret named {{ .Values.secret.existingSecret }} with required values such as:
   RAG_API_KEY, LLM_API_KEY, POSTGRES_DSN, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY.
2. Point values.yaml endpoints at your managed or in-cluster dependencies.
3. Install:
   helm upgrade --install {{ .Release.Name }} ./charts/enterprise-rag
